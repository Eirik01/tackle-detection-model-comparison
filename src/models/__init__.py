"""Neural network architectures for the three thesis pipelines.

Active heads live under `dinov3/` (linear_probe, attentive_probe) and
`vjepa2/` (attentive_pooler). The legacy `TackleActionSpotter` and related
spotter heads have moved to `legacy/src/models/`.
"""
